{
  "name": "gather-links",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Premiere link generator — auto-gathers Apple, YouTube and Spotify links and builds a ready-to-post announcement.",
  "scripts": {
    "test": "node --test test/gather.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
